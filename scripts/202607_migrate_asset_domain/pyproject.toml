[project]
name = "migrate-asset-domain"
version = "0.1.0"
description = "Migrate R2 asset URLs from skydiiv.space to assets.skydiiv.space"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.1",
]
