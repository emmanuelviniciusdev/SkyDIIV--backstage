[project]
name = "migrate-r2-production-assets"
version = "0.1.0"
description = "Copy R2 objects to skydiiv--production--assets and rewrite clothing_items.image_url paths"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.1",
]
